思路是用 opengl 做一个布料掉在球上的仿真；

目标：
    All done.

已完成：
    建立 OpenGL 窗口；
    绘制地板；
    加入摄像机移动；
    绘制球体（中心位于原点）；
    Cloth 类中加入速度数组与力的数组（类似 particle_positons）；
    为布料分配质点；
    创建两个布料的状态，每次运动状态由旧状态转移而来；
    绘制布料；
    正反面使用不同的颜色；
    完成 drop 函数（先不考虑固定与碰撞）；
    加入弹力与重力计算；
    加入固定；
    float 精度不理想，用 double 进行计算；
    加入阻尼力计算；
    加入地板碰撞检测（法线方向速度清零）；
    加入球体碰撞检测（法线方向速度清零）；
    加入释放逻辑；
    空格键的还原存在问题（四角未锁，速度未置 0）；
    为 ball 加入平行光源光照；
    进阶：一阶运动时的一阶欧拉算法换为四阶龙格库塔积分；
